package com.ultimatelld.theory.module02patterns.creational;

/** The set of delivery channels the {@link NotificationFactory} knows how to build. */
public enum Channel {
    EMAIL,
    SMS,
    PUSH
}
