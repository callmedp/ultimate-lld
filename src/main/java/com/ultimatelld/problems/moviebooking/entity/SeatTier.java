package com.ultimatelld.problems.moviebooking.entity;

/** Seat pricing tier. Pricing itself is resolved by a SeatPricing strategy (OCP). */
public enum SeatTier {
    SILVER,
    GOLD,
    PLATINUM
}
